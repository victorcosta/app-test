<?xml version="1.0" encoding="UTF-8"?>

<!-- config.xml reference: https://build.phonegap.com/docs/config-xml -->
<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id        = "com.phonegap.hello-world"
        version   = "1.0.0">

    <name>App-test</name>

    <author href="http://trilh.us" email="victorcostadev@gmail.com">
        Victor Costa
    </author>

    <!--
        Enable individual API permissions by defining each here.
        The 'device' permission is required for the 'deviceready' event.
    -->
    <feature name="http://api.phonegap.com/1.0/device" />

    <!--
        If you do not want any permissions to be added to your app, add the
        following tag to your config.xml; you will still have the INTERNET
        permission on your app, which PhoneGap requires.
    -->
    <!-- <preference name="permissions"                value="all"/> -->

    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version"           value="2.5.0" />          <!-- all: current version of PhoneGap -->
    <preference name="orientation"                value="default" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="false" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="true" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="true" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="true" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="7" />             <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->


    <!-- Plugins can also be added here. -->
    <!--
        <gap:plugin name="Example" />
        A list of available plugins are available at https://build.phonegap.com/docs/plugins
    -->
<!--     <plugin name="Accelerometer" value="org.apache.cordova.AccelListener" />
    <plugin name="Accelerometer" value="CDVAccelerometer" />
    <plugin name="Camera" value="org.apache.cordova.CameraLauncher" />
    <plugin name="Camera" value="CDVCamera" />
    <plugin name="Compass" value="org.apache.cordova.CompassListener" />
    <plugin name="Compass" value="CDVLocation" />
    <plugin name="NetworkStatus" value="org.apache.cordova.NetworkManager" />
    <plugin name="NetworkStatus" value="CDVConnection" />
    <plugin name="Notification" value="org.apache.cordova.Notification"/>
    <plugin name="Notification" value="CDVNotification" />
    <plugin name="SplashScreen" value="org.apache.cordova.SplashScreen"/>
    <plugin name="SplashScreen" value="CDVSplashScreen" /> -->
    <plugins>
	    <plugin name="App" value="org.apache.cordova.App"/>
	    <plugin name="Geolocation" value="org.apache.cordova.GeoBroker"/>
	    <plugin name="Device" value="org.apache.cordova.Device"/>
	    <plugin name="Accelerometer" value="org.apache.cordova.AccelListener"/>
	    <plugin name="Compass" value="org.apache.cordova.CompassListener"/>
	    <plugin name="Media" value="org.apache.cordova.AudioHandler"/>
	    <plugin name="Camera" value="org.apache.cordova.CameraLauncher"/>
	    <plugin name="Contacts" value="org.apache.cordova.ContactManager"/>
	    <plugin name="File" value="org.apache.cordova.FileUtils"/>
	    <plugin name="NetworkStatus" value="org.apache.cordova.NetworkManager"/>
	    <plugin name="Notification" value="org.apache.cordova.Notification"/>
	    <plugin name="Storage" value="org.apache.cordova.Storage"/>
	    <plugin name="Temperature" value="org.apache.cordova.TempListener"/>
	    <plugin name="FileTransfer" value="org.apache.cordova.FileTransfer"/>
	    <plugin name="Capture" value="org.apache.cordova.Capture"/>
	    <plugin name="Battery" value="org.apache.cordova.BatteryListener"/>
	    <plugin name="SplashScreen" value="org.apache.cordova.SplashScreen"/>
	    <plugin name="Crypto" value="com.phonegap.CryptoHandler"/>
	    <plugin name="SimpleMenu" value="com.phonegap.menu.AppMenu"/>
	    <plugin name="SelectTextPlugin" value="org.wikipedia.beta.SelectTextPlugin" />
	    <plugin name="SoftKeyBoard" value="com.phonegap.softkeyboard.SoftKeyBoard" />
	    <plugin name="ToastPlugin" value="com.chariotsolutions.toast.plugin.ToastPlugin" />
	    <plugin name="Share" value="com.schaul.plugins.share.Share"/>
	    <plugin name="URLCache" value="com.phonegap.urlcache.URLCache"/>
	    <plugin name="ApplicationVersion" value="org.wikipedia.ApplicationVersion"/>
	    <plugin name="GlobalizationCommand" value="com.phonegap.plugins.globalization.GlobalizationCommand"/>
	    <plugin name="WebIntent" value="com.borismus.webintent.WebIntent"/>
	    <plugin name="Preferences" value="org.wikipedia.PreferencesPlugin"/>
	</plugins>


    <!-- Define app splash screen for each platform. -->
    <gap:splash src="assets/img/hdpi.png"  gap:platform="android" gap:density="ldpi" />
    <gap:splash src="assets/img/hdpi.png"  gap:platform="android" gap:density="mdpi" />
    <gap:splash src="assets/img/hdpi.png"  gap:platform="android" gap:density="hdpi" />
    <gap:splash src="assets/img/hdpi.png" gap:platform="android" gap:density="xhdpi" />
    <gap:splash src="assets/img/hdpi.png"         gap:platform="blackberry" />
    <gap:splash src="assets/img/hdpi.png"    gap:platform="ios"     width="320" height="480" />
    <gap:splash src="assets/img/hdpi.png" gap:platform="ios"     width="640" height="960" />
    <gap:splash src="assets/img/hdpi.png"      gap:platform="ios"     width="768" height="1024" />
    <gap:splash src="assets/img/hdpi.png"     gap:platform="ios"     width="1024" height="768" />
    <gap:splash src="assets/img/hdpi.png" gap:platform="winphone" />


    <!--
        Define access to external domains.

        <access />            - a blank access tag denies access to all external resources.
        <access origin="*" /> - a wildcard access tag allows access to all external resource.

        Otherwise, you can specify specific domains:
    -->

    <!--
        <access origin="http://phonegap.com" />                    - allow any secure requests to http://phonegap.com/
        <access origin="http://phonegap.com" subdomains="true" />  - same as above, but including subdomains, such as http://build.phonegap.com/
        <access origin="http://phonegap.com" browserOnly="true" /> - only allows http://phonegap.com to be opened by the child browser.
    -->

</widget>